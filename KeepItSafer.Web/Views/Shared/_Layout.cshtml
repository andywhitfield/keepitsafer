<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="@Url.Content("~/favicon.ico")?v=2" />
    <title>KeepItSafer</title>

    <environment names="Development">
        <link rel="stylesheet" href="~/css/keepitsafer.css" />
    </environment>
    <environment names="Staging,Production">
        <link rel="stylesheet" href="~/css/keepitsafer.min.css" asp-append-version="true" />
    </environment>
</head>
<body>
    <nav>
       <a asp-area="" asp-controller="Home" asp-action="Index"><img src="@Url.Content("~/images/keepitsafer.png")" width="32" height="32" alt="KeepItSafer" /><h1>KeepItSafer</h1></a>
        <span class="actions">
        @if (User != null && User.Identity.IsAuthenticated) {
            <text>
            <a href="@Url.Content("~/signout")">Logout</a>
            </text>
        } else {
            <a href="@Url.Content("~/signin")">Login</a>
        }
        </span>
    </nav>
    
    @RenderBody()
       
    <footer>
        <p>&copy; @DateTime.UtcNow.Year ~ KeepItSafer</p>
    </footer>

    <environment names="Development">
        <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.5.1.js"></script>
        <script src="~/js/keepitsafer.js" asp-append-version="true"></script>
    </environment>
    <environment names="Staging,Production">
        <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.5.1.min.js"></script>
        <script src="~/js/keepitsafer.min.js" asp-append-version="true"></script>
    </environment>

    @RenderSection("Scripts", required: false)
</body>
</html>
